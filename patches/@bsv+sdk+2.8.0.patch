diff --git a/node_modules/@bsv/sdk/dist/cjs/src/primitives/AESGCM.js b/node_modules/@bsv/sdk/dist/cjs/src/primitives/AESGCM.js
index 051de10..9c37cb4 100644
--- a/node_modules/@bsv/sdk/dist/cjs/src/primitives/AESGCM.js
+++ b/node_modules/@bsv/sdk/dist/cjs/src/primitives/AESGCM.js
@@ -462,9 +462,6 @@ function AESGCM(plainText, initializationVector, key) {
     };
 }
 function AESGCMDecrypt(cipherText, initializationVector, authenticationTag, key) {
-    if (cipherText.length === 0) {
-        throw new Error('Cipher text must not be empty');
-    }
     if (initializationVector.length === 0) {
         throw new Error('Initialization vector must not be empty');
     }
diff --git a/node_modules/@bsv/sdk/dist/cjs/src/primitives/ECDSA.js b/node_modules/@bsv/sdk/dist/cjs/src/primitives/ECDSA.js
index 34b8cce..7917477 100644
--- a/node_modules/@bsv/sdk/dist/cjs/src/primitives/ECDSA.js
+++ b/node_modules/@bsv/sdk/dist/cjs/src/primitives/ECDSA.js
@@ -9,6 +9,7 @@ const Signature_js_1 = __importDefault(require("./Signature.js"));
 const Curve_js_1 = __importDefault(require("./Curve.js"));
 const Point_js_1 = require("./Point.js");
 const DRBG_js_1 = __importDefault(require("./DRBG.js"));
+const NativeSecp_js_1 = require("./NativeSecp.js");
 /**
  * Truncates a BigNumber message to the length of the curve order n, in the context of the Elliptic Curve Digital Signature Algorithm (ECDSA).
  * This method is used as part of ECDSA signing and verification.
@@ -131,6 +132,28 @@ const sign = (msg, key, forceLowS = false, customK) => {
     if (msg.bitLength() > nBitLength) {
         throw new Error(`ECDSA message is too large: expected <= ${nBitLength} bits. Callers must hash messages before signing.`);
     }
+    // Native/noble route (BSV Browser patch): RFC 6979 + low-S ECDSA — libsecp256k1
+    // when the Nitro module is present, else the always-on @noble/secp256k1 backend
+    // (CR-7, issue #35: web/jest/Expo Go). Both are byte-exact vs this implementation
+    // when forceLowS=true and no customK. Any backend failure falls through to JS.
+    {
+        const n = (0, NativeSecp_js_1.secpBackend)();
+        if (n != null) {
+            if (customK != null) {
+                (0, NativeSecp_js_1.warnCustomKOnce)();
+            }
+            else if (forceLowS !== true) {
+                (0, NativeSecp_js_1.warnForceLowSOnce)();
+            }
+            else {
+                try {
+                    const der = n.ecdsaSign((0, NativeSecp_js_1.bnToBuf32)(msg), (0, NativeSecp_js_1.bnToBuf32)(key));
+                    return Signature_js_1.default.fromDER(Array.from(new Uint8Array(der)));
+                }
+                catch (e) { /* fall through to pure-JS */ }
+            }
+        }
+    }
     msg = truncateToN(msg);
     const msgBig = bnToBigInt(msg);
     const keyBig = bnToBigInt(key);
@@ -183,6 +206,19 @@ const verify = (msg, sig, key) => {
     if (key.x == null || key.y == null) {
         throw new Error('Invalid public key: missing coordinates.');
     }
+    // Native route (BSV Browser patch): libsecp256k1 verify with S normalized first —
+    // semantics equal to this implementation (accepts any s in [1, n-1]). Any native
+    // failure (e.g. a malformed sig/pubkey for strict parsers) falls through to the
+    // pure-JS path so edge-case behaviour is preserved.
+    {
+        const n = (0, NativeSecp_js_1.nativeSecp)();
+        if (n != null && key.validate()) {
+            try {
+                return n.ecdsaVerify((0, NativeSecp_js_1.bnToBuf32)(msg), (0, NativeSecp_js_1.numsToBuf)(sig.toDER()), (0, NativeSecp_js_1.numsToBuf)(key.encode(true)));
+            }
+            catch (e) { /* fall through to pure-JS */ }
+        }
+    }
     const publicKey = {
         x: bnToBigInt(key.x),
         y: bnToBigInt(key.y)
diff --git a/node_modules/@bsv/sdk/dist/cjs/src/primitives/NativeSecp.js b/node_modules/@bsv/sdk/dist/cjs/src/primitives/NativeSecp.js
new file mode 100644
index 0000000..2cedd4b
--- /dev/null
+++ b/node_modules/@bsv/sdk/dist/cjs/src/primitives/NativeSecp.js
@@ -0,0 +1,195 @@
+"use strict";
+/**
+ * BSV Browser native secp seam — added by patch-package (native-crypto M2).
+ *
+ * The app entry (index.js) installs a Nitro hybrid object (react-native-secp-native,
+ * backed by the rust-secp256k1 staticlib proven byte-exact against this exact SDK
+ * version in native-secp-poc/) at `globalThis.__bsvSecpNative`. When it is absent
+ * (web, jest, node, Expo Go, or any build without the pod) every caller silently
+ * uses the original pure-JS implementation — same probe philosophy as Hash.ts's
+ * node:crypto detection.
+ *
+ * Bytes-only seam: all BigNumber/Point/DER conversion happens HERE, at the
+ * boundary, and nowhere else. No hex strings on the hot path.
+ */
+Object.defineProperty(exports, "__esModule", { value: true });
+exports.nativeSecp = nativeSecp;
+exports.nobleSecp = nobleSecp;
+exports.secpBackend = secpBackend;
+exports.nativeEngine = nativeEngine;
+exports.bnToBuf32 = bnToBuf32;
+exports.numsToBuf = numsToBuf;
+exports.bufToNums = bufToNums;
+exports.warnCustomKOnce = warnCustomKOnce;
+exports.warnForceLowSOnce = warnForceLowSOnce;
+exports.warnEngineFallback = warnEngineFallback;
+/**
+ * Returns the native module, or null when unavailable. Never throws.
+ */
+function nativeSecp() {
+    const g = globalThis;
+    const n = g == null ? null : g.__bsvSecpNative;
+    return n == null ? null : n;
+}
+// ── Tier-3 ECDSA backend: @noble/secp256k1 (CR-7, issue #35) ─────────────────
+// An always-on, audited pure-JS secp256k1 that stands in for the Nitro native
+// module on every platform the native path can't reach (web, jest, Expo Go,
+// unlinked builds). Same probe philosophy as `nativeSecp`: the loader NEVER
+// throws — on any failure the callers fall through to the SDK's own pure-JS EC,
+// so noble is an ACCELERATOR, never a dependency. Exposes exactly the native
+// method names the seam routes through (`ecdsaSign`, `pubkeyCreate[...]`), so
+// the caller code is identical whether the backend is Nitro or noble. Byte
+// parity vs the SDK (RFC 6979 low-S DER) is gated by jest + the differential
+// corpus (the fuzz oracle signs through this exact backend).
+let nobleTried = false;
+let nobleBackend = null;
+/** Minimal canonical DER integer: trim leading zeros, prepend 0x00 if MSB set. */
+function derInt(b32) {
+    let i = 0;
+    while (i < b32.length - 1 && b32[i] === 0)
+        i++;
+    let v = b32.subarray(i);
+    if ((v[0] & 0x80) !== 0) {
+        const t = new Uint8Array(v.length + 1);
+        t.set(v, 1);
+        v = t;
+    }
+    return v;
+}
+/** Canonical DER SEQUENCE(INTEGER r, INTEGER s) from two 32-byte big-endian scalars. */
+function derFromRS(r32, s32) {
+    const R = derInt(r32);
+    const S = derInt(s32);
+    const body = 2 + R.length + 2 + S.length; // always < 128 → single-byte lengths
+    const out = new Uint8Array(2 + body);
+    let o = 0;
+    out[o++] = 0x30;
+    out[o++] = body;
+    out[o++] = 0x02;
+    out[o++] = R.length;
+    out.set(R, o);
+    o += R.length;
+    out[o++] = 0x02;
+    out[o++] = S.length;
+    out.set(S, o);
+    return out;
+}
+/**
+ * Returns the noble ECDSA backend, or null if it cannot be loaded/initialised.
+ * Lazy + memoised + fully guarded (a require of an ESM-only build, a missing
+ * install, or any init error → null → pure-JS). RFC 6979 HMAC-SHA256 is wired to
+ * the SDK's OWN `sha256hmac` so the deterministic nonce is identical to the SDK
+ * DRBG and works in every runtime (no WebCrypto dependency).
+ */
+function nobleSecp() {
+    if (nobleTried)
+        return nobleBackend;
+    nobleTried = true;
+    try {
+        // eslint-disable-next-line @typescript-eslint/no-var-requires
+        const secp = require('@noble/secp256k1');
+        if (secp == null || typeof secp.sign !== 'function' || typeof secp.getPublicKey !== 'function') {
+            nobleBackend = null;
+            return nobleBackend;
+        }
+        if (secp.etc != null && typeof secp.etc.hmacSha256Sync !== 'function') {
+            // eslint-disable-next-line @typescript-eslint/no-var-requires
+            const Hash = require('./Hash.js');
+            secp.etc.hmacSha256Sync = (key, ...msgs) => Uint8Array.from(Hash.sha256hmac(Array.from(key), Array.from(secp.etc.concatBytes(...msgs))));
+        }
+        nobleBackend = {
+            __noble: true,
+            // DER‖(no scope) — matches the native module's ecdsaSign contract.
+            ecdsaSign(msgBuf, keyBuf) {
+                const sig = secp.sign(new Uint8Array(msgBuf), new Uint8Array(keyBuf), { lowS: true });
+                const c = sig.toCompactRawBytes();
+                return derFromRS(c.subarray(0, 32), c.subarray(32, 64));
+            },
+            // 65-byte uncompressed 0x04‖x‖y — matches pubkeyCreateUncompressed.
+            pubkeyCreateUncompressed(keyBuf) {
+                return secp.getPublicKey(new Uint8Array(keyBuf), false);
+            },
+            // 33-byte compressed — matches pubkeyCreate.
+            pubkeyCreate(keyBuf) {
+                return secp.getPublicKey(new Uint8Array(keyBuf), true);
+            }
+        };
+    }
+    catch (e) {
+        nobleBackend = null;
+    }
+    return nobleBackend;
+}
+/**
+ * The ECDSA backend the sign / toPublicKey seams route through: the Nitro native
+ * module when present (fastest), else the always-on noble backend, else null →
+ * the caller's pure-JS EC. Never throws.
+ */
+function secpBackend() {
+    return nativeSecp() ?? nobleSecp();
+}
+/**
+ * Returns the Tier-3 native engine (react-native-engine-native, installed by
+ * index.js at `globalThis.__bsvEngineNative` — native-crypto M5.5), or null
+ * when unavailable or lacking the batch sign entry point. Never throws.
+ */
+function nativeEngine() {
+    const g = globalThis;
+    const n = g == null ? null : g.__bsvEngineNative;
+    if (n == null || typeof n.batchSignP2pkhInputs !== 'function')
+        return null;
+    return n;
+}
+/**
+ * BigNumber (value < 2^256) → 32-byte big-endian ArrayBuffer.
+ */
+function bnToBuf32(bn) {
+    return Uint8Array.from(bn.toArray('be', 32)).buffer;
+}
+/**
+ * number[] → ArrayBuffer.
+ */
+function numsToBuf(nums) {
+    return Uint8Array.from(nums).buffer;
+}
+/**
+ * ArrayBuffer → number[].
+ */
+function bufToNums(buf) {
+    return Array.from(new Uint8Array(buf));
+}
+let warnedCustomK = false;
+let warnedForceLowS = false;
+/**
+ * ECDSA.sign was called with a customK — the native RFC 6979 path cannot honor
+ * it, so the caller uses the (tested) pure-JS fallback. Warn once, dev builds only.
+ */
+function warnCustomKOnce() {
+    if (!warnedCustomK && typeof __DEV__ !== 'undefined' && __DEV__) {
+        warnedCustomK = true;
+        console.warn('[NativeSecp] ECDSA.sign called with customK — using pure-JS fallback for such calls (native RFC 6979 path bypassed).');
+    }
+}
+/**
+ * ECDSA.sign was called with forceLowS=false — libsecp256k1 always emits low-S,
+ * so byte-parity with the JS high-S output cannot be guaranteed; the caller uses
+ * the pure-JS fallback. Warn once, dev builds only.
+ */
+function warnForceLowSOnce() {
+    if (!warnedForceLowS && typeof __DEV__ !== 'undefined' && __DEV__) {
+        warnedForceLowS = true;
+        console.warn('[NativeSecp] ECDSA.sign called with forceLowS=false — using pure-JS fallback for such calls (native path is always low-S).');
+    }
+}
+const warnedEngineReasons = {};
+/**
+ * Transaction.sign could not (or did not) route through the Tier-3 engine and
+ * used the SecpNative-batched / pure-JS fallback tiers instead (gate 6: every
+ * fallback visibly logged in dev builds). Logged once per distinct reason.
+ */
+function warnEngineFallback(reason) {
+    if (warnedEngineReasons[reason] !== true && typeof __DEV__ !== 'undefined' && __DEV__) {
+        warnedEngineReasons[reason] = true;
+        console.warn(`[EngineNative] Transaction.sign fell back from the engine-routed path: ${reason}`);
+    }
+}
diff --git a/node_modules/@bsv/sdk/dist/cjs/src/primitives/PrivateKey.js b/node_modules/@bsv/sdk/dist/cjs/src/primitives/PrivateKey.js
index d80b162..378d987 100644
--- a/node_modules/@bsv/sdk/dist/cjs/src/primitives/PrivateKey.js
+++ b/node_modules/@bsv/sdk/dist/cjs/src/primitives/PrivateKey.js
@@ -45,6 +45,8 @@ const Hash_js_1 = require("./Hash.js");
 const Random_js_1 = __importDefault(require("./Random.js"));
 const utils_js_1 = require("./utils.js");
 const Polynomial_js_1 = __importStar(require("./Polynomial.js"));
+const Point_js_1 = __importDefault(require("./Point.js"));
+const NativeSecp_js_1 = require("./NativeSecp.js");
 const MAX_BACKUP_SHARE_LENGTH = 256;
 const BACKUP_SHARE_FIELD = /^[1-9A-HJ-NP-Za-km-z]{1,64}$/;
 const BACKUP_SHARE_THRESHOLD = /^(?:[2-9]|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])$/;
@@ -314,6 +316,24 @@ class PrivateKey extends BigNumber_js_1.default {
      * const publicKey = privateKey.toPublicKey();
      */
     toPublicKey() {
+        // Native/noble route (BSV Browser patch): libsecp256k1 pubkey_create when
+        // the Nitro module is present, else the always-on @noble/secp256k1 backend
+        // (CR-7, issue #35). M3: prefer the uncompressed variant (0x04‖x‖y,
+        // byte-equal to encode(false)) so the seam never pays the Point.fromX
+        // BigInt-sqrt decompression; fall back to the compressed fn + fromDER on
+        // older native modules. Falls through to pure-JS on any backend failure
+        // (e.g. key === 0 → point at infinity, which noble rejects).
+        const n = (0, NativeSecp_js_1.secpBackend)();
+        if (n != null) {
+            try {
+                if (typeof n.pubkeyCreateUncompressed === 'function') {
+                    const u = (0, NativeSecp_js_1.bufToNums)(n.pubkeyCreateUncompressed((0, NativeSecp_js_1.bnToBuf32)(this)));
+                    return new PublicKey_js_1.default(u.slice(1, 33), u.slice(33, 65));
+                }
+                return PublicKey_js_1.default.fromDER((0, NativeSecp_js_1.bufToNums)(n.pubkeyCreate((0, NativeSecp_js_1.bnToBuf32)(this))));
+            }
+            catch (e) { /* fall through to pure-JS */ }
+        }
         const c = new Curve_js_1.default();
         const p = c.g.mulCT(this);
         return new PublicKey_js_1.default(p.x, p.y);
@@ -402,6 +422,21 @@ class PrivateKey extends BigNumber_js_1.default {
         if (!key.validate()) {
             throw new Error('Public key not valid for ECDH secret derivation');
         }
+        // Native route (BSV Browser patch): un-hashed ECDH shared point via
+        // libsecp256k1. M3: prefer the uncompressed variant (no BigInt-sqrt
+        // decompression at the seam). Falls through to pure-JS on any native
+        // failure so edge-case behavior is preserved.
+        const n = (0, NativeSecp_js_1.nativeSecp)();
+        if (n != null) {
+            try {
+                if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                    const u = (0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPointUncompressed((0, NativeSecp_js_1.bnToBuf32)(this), (0, NativeSecp_js_1.numsToBuf)(key.encode(true))));
+                    return new Point_js_2.default(u.slice(1, 33), u.slice(33, 65));
+                }
+                return Point_js_2.default.fromDER((0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPoint((0, NativeSecp_js_1.bnToBuf32)(this), (0, NativeSecp_js_1.numsToBuf)(key.encode(true)))));
+            }
+            catch (e) { /* fall through to pure-JS */ }
+        }
         return key.mulCT(this);
     }
     /**
@@ -440,6 +475,59 @@ class PrivateKey extends BigNumber_js_1.default {
      * @returns The derived child key.
      */
     deriveChild(publicKey, invoiceNumber, cacheSharedSecret, retrieveCachedSharedSecret) {
+        // Native route (BSV Browser patch): single-call BRC-42 composite
+        // (ECDH point → HMAC-SHA256 → (k + tweak) mod n) in libsecp256k1 when
+        // no shared-secret cache callbacks are supplied. M3: when cache
+        // callbacks ARE supplied (e.g. CachedKeyDeriver), the caching contract
+        // is honored — the JS Point flows through the callbacks exactly as in
+        // the pure-JS path — while ECDH and the tweak-add stay native (M2 fell
+        // back to pure JS here). Falls through to pure-JS on any native failure
+        // (incl. hmac >= n / child == 0, where the JS mod-n semantics differ).
+        {
+            const n = (0, NativeSecp_js_1.nativeSecp)();
+            if (n != null && typeof invoiceNumber === 'string' && publicKey.validate()) {
+                try {
+                    if (typeof cacheSharedSecret !== 'function' && typeof retrieveCachedSharedSecret !== 'function') {
+                        const child = n.brc42DeriveChild((0, NativeSecp_js_1.bnToBuf32)(this), (0, NativeSecp_js_1.numsToBuf)(publicKey.encode(true)), invoiceNumber);
+                        return new PrivateKey((0, NativeSecp_js_1.bufToNums)(child));
+                    }
+                    let sharedSecretN;
+                    if (typeof retrieveCachedSharedSecret === 'function') {
+                        const retrieved = retrieveCachedSharedSecret(this, publicKey);
+                        if (retrieved === undefined) {
+                            if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                                const u = (0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPointUncompressed((0, NativeSecp_js_1.bnToBuf32)(this), (0, NativeSecp_js_1.numsToBuf)(publicKey.encode(true))));
+                                sharedSecretN = new Point_js_2.default(u.slice(1, 33), u.slice(33, 65));
+                            }
+                            else {
+                                sharedSecretN = Point_js_2.default.fromDER((0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPoint((0, NativeSecp_js_1.bnToBuf32)(this), (0, NativeSecp_js_1.numsToBuf)(publicKey.encode(true)))));
+                            }
+                            if (typeof cacheSharedSecret === 'function') {
+                                cacheSharedSecret(this, publicKey, sharedSecretN);
+                            }
+                        }
+                        else {
+                            sharedSecretN = retrieved;
+                        }
+                    }
+                    else {
+                        // only cacheSharedSecret supplied: compute natively, cache
+                        if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                            const u = (0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPointUncompressed((0, NativeSecp_js_1.bnToBuf32)(this), (0, NativeSecp_js_1.numsToBuf)(publicKey.encode(true))));
+                            sharedSecretN = new Point_js_2.default(u.slice(1, 33), u.slice(33, 65));
+                        }
+                        else {
+                            sharedSecretN = Point_js_2.default.fromDER((0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPoint((0, NativeSecp_js_1.bnToBuf32)(this), (0, NativeSecp_js_1.numsToBuf)(publicKey.encode(true)))));
+                        }
+                        cacheSharedSecret(this, publicKey, sharedSecretN);
+                    }
+                    const hmacN = (0, Hash_js_1.sha256hmac)(sharedSecretN.encode(true), (0, utils_js_1.toArray)(invoiceNumber, 'utf8'));
+                    const child = n.privkeyTweakAdd((0, NativeSecp_js_1.bnToBuf32)(this), (0, NativeSecp_js_1.numsToBuf)(hmacN));
+                    return new PrivateKey((0, NativeSecp_js_1.bufToNums)(child));
+                }
+                catch (e) { /* fall through to pure-JS */ }
+            }
+        }
         let sharedSecret;
         if (typeof retrieveCachedSharedSecret === 'function') {
             const retrieved = retrieveCachedSharedSecret(this, publicKey);
diff --git a/node_modules/@bsv/sdk/dist/cjs/src/primitives/PublicKey.js b/node_modules/@bsv/sdk/dist/cjs/src/primitives/PublicKey.js
index 6e15dd5..cd1d3a9 100644
--- a/node_modules/@bsv/sdk/dist/cjs/src/primitives/PublicKey.js
+++ b/node_modules/@bsv/sdk/dist/cjs/src/primitives/PublicKey.js
@@ -10,6 +10,7 @@ const BigNumber_js_1 = __importDefault(require("./BigNumber.js"));
 const Hash_js_1 = require("./Hash.js");
 const Signature_js_1 = __importDefault(require("./Signature.js"));
 const utils_js_1 = require("./utils.js");
+const NativeSecp_js_1 = require("./NativeSecp.js");
 /**
  * The PublicKey class extends the Point class. It is used in public-key cryptography to derive shared secret, verify message signatures, and encode the public key in the DER format.
  * The class comes with static methods to generate PublicKey instances from private keys or from strings.
@@ -108,6 +109,21 @@ class PublicKey extends Point_js_1.default {
         if (!this.validate()) {
             throw new Error('Public key not valid for ECDH secret derivation');
         }
+        // Native route (BSV Browser patch): un-hashed ECDH shared point via
+        // libsecp256k1. M3: prefer the uncompressed variant (no BigInt-sqrt
+        // decompression at the seam). Falls through to pure-JS on any native
+        // failure.
+        const n = (0, NativeSecp_js_1.nativeSecp)();
+        if (n != null) {
+            try {
+                if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                    const u = (0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPointUncompressed((0, NativeSecp_js_1.bnToBuf32)(priv), (0, NativeSecp_js_1.numsToBuf)(this.encode(true))));
+                    return new Point_js_1.default(u.slice(1, 33), u.slice(33, 65));
+                }
+                return Point_js_1.default.fromDER((0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPoint((0, NativeSecp_js_1.bnToBuf32)(priv), (0, NativeSecp_js_1.numsToBuf)(this.encode(true)))));
+            }
+            catch (e) { /* fall through to pure-JS */ }
+        }
         return this.mulCT(priv);
     }
     /**
@@ -195,6 +211,68 @@ class PublicKey extends Point_js_1.default {
      * @returns The derived child key.
      */
     deriveChild(privateKey, invoiceNumber, cacheSharedSecret, retrieveCachedSharedSecret) {
+        // Native route (BSV Browser patch): BRC-42 public-side derivation.
+        // M3: when no cache callbacks are supplied, ONE native composite call
+        // (brc42DeriveChildPubUncompressed: ECDH → HMAC → P + t·G, uncompressed
+        // out — no decompression, one crossing instead of three). When cache
+        // callbacks ARE supplied (e.g. CachedKeyDeriver), the caching contract
+        // is honored with the JS Point flowing through the callbacks while the
+        // point math stays native. Falls through to pure-JS on any native failure.
+        {
+            const n = (0, NativeSecp_js_1.nativeSecp)();
+            if (n != null && typeof invoiceNumber === 'string' && this.validate()) {
+                try {
+                    const self33 = (0, NativeSecp_js_1.numsToBuf)(this.encode(true));
+                    if (typeof cacheSharedSecret !== 'function' && typeof retrieveCachedSharedSecret !== 'function') {
+                        if (typeof n.brc42DeriveChildPubUncompressed === 'function') {
+                            const u = (0, NativeSecp_js_1.bufToNums)(n.brc42DeriveChildPubUncompressed((0, NativeSecp_js_1.bnToBuf32)(privateKey), self33, invoiceNumber));
+                            return new PublicKey(u.slice(1, 33), u.slice(33, 65));
+                        }
+                        const shared = (0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPoint((0, NativeSecp_js_1.bnToBuf32)(privateKey), self33));
+                        const hmac = (0, Hash_js_1.sha256hmac)(shared, (0, utils_js_1.toArray)(invoiceNumber, 'utf8'));
+                        const tweaked = n.pubkeyTweakAdd(self33, (0, NativeSecp_js_1.numsToBuf)(hmac));
+                        return PublicKey.fromDER((0, NativeSecp_js_1.bufToNums)(tweaked));
+                    }
+                    let sharedSecretN;
+                    if (typeof retrieveCachedSharedSecret === 'function') {
+                        const retrieved = retrieveCachedSharedSecret(privateKey, this);
+                        if (retrieved === undefined) {
+                            if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                                const u = (0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPointUncompressed((0, NativeSecp_js_1.bnToBuf32)(privateKey), self33));
+                                sharedSecretN = new Point_js_1.default(u.slice(1, 33), u.slice(33, 65));
+                            }
+                            else {
+                                sharedSecretN = Point_js_1.default.fromDER((0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPoint((0, NativeSecp_js_1.bnToBuf32)(privateKey), self33)));
+                            }
+                            if (typeof cacheSharedSecret === 'function') {
+                                cacheSharedSecret(privateKey, this, sharedSecretN);
+                            }
+                        }
+                        else {
+                            sharedSecretN = retrieved;
+                        }
+                    }
+                    else {
+                        if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                            const u = (0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPointUncompressed((0, NativeSecp_js_1.bnToBuf32)(privateKey), self33));
+                            sharedSecretN = new Point_js_1.default(u.slice(1, 33), u.slice(33, 65));
+                        }
+                        else {
+                            sharedSecretN = Point_js_1.default.fromDER((0, NativeSecp_js_1.bufToNums)(n.ecdhSharedPoint((0, NativeSecp_js_1.bnToBuf32)(privateKey), self33)));
+                        }
+                        cacheSharedSecret(privateKey, this, sharedSecretN);
+                    }
+                    const hmacN = (0, Hash_js_1.sha256hmac)(sharedSecretN.encode(true), (0, utils_js_1.toArray)(invoiceNumber, 'utf8'));
+                    if (typeof n.pubkeyTweakAddUncompressed === 'function') {
+                        const u = (0, NativeSecp_js_1.bufToNums)(n.pubkeyTweakAddUncompressed(self33, (0, NativeSecp_js_1.numsToBuf)(hmacN)));
+                        return new PublicKey(u.slice(1, 33), u.slice(33, 65));
+                    }
+                    const tweakedN = n.pubkeyTweakAdd(self33, (0, NativeSecp_js_1.numsToBuf)(hmacN));
+                    return PublicKey.fromDER((0, NativeSecp_js_1.bufToNums)(tweakedN));
+                }
+                catch (e) { /* fall through to pure-JS */ }
+            }
+        }
         let sharedSecret;
         if (typeof retrieveCachedSharedSecret === 'function') {
             const retrieved = retrieveCachedSharedSecret(privateKey, this);
diff --git a/node_modules/@bsv/sdk/dist/cjs/src/primitives/Schnorr.js b/node_modules/@bsv/sdk/dist/cjs/src/primitives/Schnorr.js
index 009f580..b8b8e40 100644
--- a/node_modules/@bsv/sdk/dist/cjs/src/primitives/Schnorr.js
+++ b/node_modules/@bsv/sdk/dist/cjs/src/primitives/Schnorr.js
@@ -7,6 +7,9 @@ const BigNumber_js_1 = __importDefault(require("./BigNumber.js"));
 const Curve_js_1 = __importDefault(require("./Curve.js"));
 const Hash_js_1 = require("./Hash.js");
 const PrivateKey_js_1 = __importDefault(require("./PrivateKey.js"));
+const Point_js_1 = __importDefault(require("./Point.js"));
+const PublicKey_js_1 = __importDefault(require("./PublicKey.js"));
+const NativeSecp_js_1 = require("./NativeSecp.js");
 /**
  * Class representing the Schnorr Zero-Knowledge Proof (ZKP) protocol.
  *
@@ -50,6 +53,27 @@ class Schnorr {
      * @returns Proof (R, S', z)
      */
     generateProof(aArg, AArg, BArg, S) {
+        // Native route (BSV Browser patch): one flow call into libsecp256k1.
+        // The nonce r is drawn HERE from PrivateKey.fromRandom — the identical
+        // source the SDK uses — and passed across the seam so the native fn
+        // stays deterministic and vector-testable. Guard: a in [1, n-1]
+        // (out-of-range/negative scalars keep exact SDK BigNumber semantics
+        // via the pure-JS path). Any native throw falls through to pure JS.
+        {
+            const nat = (0, NativeSecp_js_1.nativeSecp)();
+            if (nat != null && aArg.cmpn(0) > 0 && aArg.cmp(this.curve.n) < 0) {
+                try {
+                    const rNonce = PrivateKey_js_1.default.fromRandom();
+                    const proof = (0, NativeSecp_js_1.bufToNums)(nat.schnorrGenerateProof((0, NativeSecp_js_1.bnToBuf32)(aArg), (0, NativeSecp_js_1.numsToBuf)(AArg.encode(true)), (0, NativeSecp_js_1.numsToBuf)(BArg.encode(true)), (0, NativeSecp_js_1.numsToBuf)(S.encode(true)), (0, NativeSecp_js_1.bnToBuf32)(rNonce)));
+                    return {
+                        R: PublicKey_js_1.default.fromDER(proof.slice(0, 33)),
+                        SPrime: Point_js_1.default.fromDER(proof.slice(33, 66)),
+                        z: new BigNumber_js_1.default(proof.slice(66, 98))
+                    };
+                }
+                catch (err) { /* fall through to pure-JS */ }
+            }
+        }
         const r = PrivateKey_js_1.default.fromRandom();
         const R = r.toPublicKey();
         const SPrime = BArg.mul(r);
@@ -67,6 +91,22 @@ class Schnorr {
      */
     verifyProof(A, B, S, proof) {
         const { R, SPrime, z } = proof;
+        // Native route (BSV Browser patch): one flow call replaces four JS
+        // point multiplications. Routed ONLY for z in [1, n-1] — MEASURED: the
+        // SDK's Point.mul is not consistently (z mod n)*G for out-of-range
+        // scalars (g.mul(n+1) matches the reduced value, g.mul(2^256-1) does
+        // not), so pre-reducing z would change the SDK's verdict; out-of-range
+        // z uses the pure-JS path. Any native throw (invalid encodings, point
+        // at infinity in encode) also falls through.
+        {
+            const nat = (0, NativeSecp_js_1.nativeSecp)();
+            if (nat != null && z != null && z.cmpn(0) > 0 && z.cmp(this.curve.n) < 0) {
+                try {
+                    return nat.schnorrVerifyProof((0, NativeSecp_js_1.numsToBuf)(A.encode(true)), (0, NativeSecp_js_1.numsToBuf)(B.encode(true)), (0, NativeSecp_js_1.numsToBuf)(S.encode(true)), (0, NativeSecp_js_1.numsToBuf)(R.encode(true)), (0, NativeSecp_js_1.numsToBuf)(SPrime.encode(true)), (0, NativeSecp_js_1.bnToBuf32)(z));
+                }
+                catch (err) { /* fall through to pure-JS */ }
+            }
+        }
         const e = this.computeChallenge(A, B, S, SPrime, R);
         // Check zG = R + eA
         const zG = this.curve.g.mul(z);
diff --git a/node_modules/@bsv/sdk/dist/cjs/src/primitives/Signature.js b/node_modules/@bsv/sdk/dist/cjs/src/primitives/Signature.js
index 171fc4d..667914d 100644
--- a/node_modules/@bsv/sdk/dist/cjs/src/primitives/Signature.js
+++ b/node_modules/@bsv/sdk/dist/cjs/src/primitives/Signature.js
@@ -10,6 +10,7 @@ const Hash_js_1 = require("./Hash.js");
 const utils_js_1 = require("./utils.js");
 const Point_js_1 = __importDefault(require("./Point.js"));
 const Curve_js_1 = __importDefault(require("./Curve.js"));
+const NativeSecp_js_1 = require("./NativeSecp.js");
 /**
  * Represents a digital signature.
  *
@@ -294,6 +295,34 @@ class Signature {
      * const publicKey = signature.RecoverPublicKey(0, msgHash);
      */
     RecoverPublicKey(recovery, e) {
+        // Native route (BSV Browser patch): libsecp256k1 recovery over the SDK
+        // compact layout ([31+recid, r32, s32]). Guards keep unsigned 32-byte
+        // semantics (negative r/s/e keep the SDK's BigNumber behavior via the
+        // JS path). Any native throw — including the recid-2/3 r + n >= p field
+        // overflow the SDK resolves by Point.fromX mod-p reduction — falls
+        // through to the pure-JS path, preserving exact SDK behavior.
+        {
+            const nat = (0, NativeSecp_js_1.nativeSecp)();
+            if (nat != null &&
+                Number.isInteger(recovery) && recovery >= 0 && recovery <= 3 &&
+                this.r.cmpn(0) >= 0 && this.s.cmpn(0) >= 0 && e.cmpn(0) >= 0) {
+                try {
+                    const compact = new Uint8Array(65);
+                    compact[0] = 31 + recovery;
+                    compact.set(this.r.toArray('be', 32), 1);
+                    compact.set(this.s.toArray('be', 32), 33);
+                    // M3: prefer the uncompressed variant — no BigInt-sqrt
+                    // decompression at the seam.
+                    if (typeof nat.ecdsaRecoverUncompressed === 'function') {
+                        const u = (0, NativeSecp_js_1.bufToNums)(nat.ecdsaRecoverUncompressed((0, NativeSecp_js_1.bnToBuf32)(e), compact.buffer));
+                        return new PublicKey_js_1.default(u.slice(1, 33), u.slice(33, 65));
+                    }
+                    const pk = nat.ecdsaRecover((0, NativeSecp_js_1.bnToBuf32)(e), compact.buffer);
+                    return PublicKey_js_1.default.fromDER((0, NativeSecp_js_1.bufToNums)(pk));
+                }
+                catch (err) { /* fall through to pure-JS */ }
+            }
+        }
         const r = this.r;
         const s = this.s;
         // A set LSB signifies that the y-coordinate is odd
@@ -339,6 +368,22 @@ class Signature {
      * const recovery = signature.CalculateRecoveryFactor(publicKey, msgHash);
      */
     CalculateRecoveryFactor(pubkey, msgHash) {
+        // Native route (BSV Browser patch): one native call replaces up to four
+        // JS point-recovery rounds (same 0..3 search order, same equality). A
+        // native miss (incl. the recid-2/3 asymmetry, where the SDK may match a
+        // mod-p fabricated point) falls through to the pure-JS loop.
+        {
+            const nat = (0, NativeSecp_js_1.nativeSecp)();
+            if (nat != null && this.r.cmpn(0) >= 0 && this.s.cmpn(0) >= 0 && msgHash.cmpn(0) >= 0) {
+                try {
+                    const sig64 = new Uint8Array(64);
+                    sig64.set(this.r.toArray('be', 32), 0);
+                    sig64.set(this.s.toArray('be', 32), 32);
+                    return nat.ecdsaRecoveryFactor((0, NativeSecp_js_1.bnToBuf32)(msgHash), sig64.buffer, (0, NativeSecp_js_1.numsToBuf)(pubkey.encode(true)));
+                }
+                catch (err) { /* fall through to pure-JS */ }
+            }
+        }
         for (let recovery = 0; recovery < 4; recovery++) {
             let Qprime;
             try {
diff --git a/node_modules/@bsv/sdk/dist/esm/src/primitives/AESGCM.js b/node_modules/@bsv/sdk/dist/esm/src/primitives/AESGCM.js
index a0ee35a..7ad3333 100644
--- a/node_modules/@bsv/sdk/dist/esm/src/primitives/AESGCM.js
+++ b/node_modules/@bsv/sdk/dist/esm/src/primitives/AESGCM.js
@@ -448,9 +448,6 @@ export function AESGCM(plainText, initializationVector, key) {
     };
 }
 export function AESGCMDecrypt(cipherText, initializationVector, authenticationTag, key) {
-    if (cipherText.length === 0) {
-        throw new Error('Cipher text must not be empty');
-    }
     if (initializationVector.length === 0) {
         throw new Error('Initialization vector must not be empty');
     }
diff --git a/node_modules/@bsv/sdk/dist/esm/src/primitives/ECDSA.js b/node_modules/@bsv/sdk/dist/esm/src/primitives/ECDSA.js
index 9e23ad4..6bfa02d 100644
--- a/node_modules/@bsv/sdk/dist/esm/src/primitives/ECDSA.js
+++ b/node_modules/@bsv/sdk/dist/esm/src/primitives/ECDSA.js
@@ -3,6 +3,7 @@ import Signature from './Signature.js';
 import Curve from './Curve.js';
 import { scalarMultiplyWNAF, biModInv, BI_ZERO, biModMul, GX_BIGINT, GY_BIGINT, jpAdd, N_BIGINT, modInvN, modMulN, modN } from './Point.js';
 import DRBG from './DRBG.js';
+import { nativeSecp, secpBackend, bnToBuf32, numsToBuf, warnCustomKOnce, warnForceLowSOnce } from './NativeSecp.js';
 /**
  * Truncates a BigNumber message to the length of the curve order n, in the context of the Elliptic Curve Digital Signature Algorithm (ECDSA).
  * This method is used as part of ECDSA signing and verification.
@@ -125,6 +126,28 @@ export const sign = (msg, key, forceLowS = false, customK) => {
     if (msg.bitLength() > nBitLength) {
         throw new Error(`ECDSA message is too large: expected <= ${nBitLength} bits. Callers must hash messages before signing.`);
     }
+    // Native/noble route (BSV Browser patch): RFC 6979 + low-S ECDSA — libsecp256k1
+    // when the Nitro module is present, else the always-on @noble/secp256k1 backend
+    // (CR-7, issue #35: web/jest/Expo Go). Both are byte-exact vs this implementation
+    // when forceLowS=true and no customK. Any backend failure falls through to JS.
+    {
+        const n = secpBackend();
+        if (n != null) {
+            if (customK != null) {
+                warnCustomKOnce();
+            }
+            else if (forceLowS !== true) {
+                warnForceLowSOnce();
+            }
+            else {
+                try {
+                    const der = n.ecdsaSign(bnToBuf32(msg), bnToBuf32(key));
+                    return Signature.fromDER(Array.from(new Uint8Array(der)));
+                }
+                catch (e) { /* fall through to pure-JS */ }
+            }
+        }
+    }
     msg = truncateToN(msg);
     const msgBig = bnToBigInt(msg);
     const keyBig = bnToBigInt(key);
@@ -176,6 +199,19 @@ export const verify = (msg, sig, key) => {
     if (key.x == null || key.y == null) {
         throw new Error('Invalid public key: missing coordinates.');
     }
+    // Native route (BSV Browser patch): libsecp256k1 verify with S normalized first —
+    // semantics equal to this implementation (accepts any s in [1, n-1]). Any native
+    // failure (e.g. a malformed sig/pubkey for strict parsers) falls through to the
+    // pure-JS path so edge-case behaviour is preserved.
+    {
+        const n = nativeSecp();
+        if (n != null && key.validate()) {
+            try {
+                return n.ecdsaVerify(bnToBuf32(msg), numsToBuf(sig.toDER()), numsToBuf(key.encode(true)));
+            }
+            catch (e) { /* fall through to pure-JS */ }
+        }
+    }
     const publicKey = {
         x: bnToBigInt(key.x),
         y: bnToBigInt(key.y)
diff --git a/node_modules/@bsv/sdk/dist/esm/src/primitives/NativeSecp.js b/node_modules/@bsv/sdk/dist/esm/src/primitives/NativeSecp.js
new file mode 100644
index 0000000..9672b48
--- /dev/null
+++ b/node_modules/@bsv/sdk/dist/esm/src/primitives/NativeSecp.js
@@ -0,0 +1,175 @@
+/**
+ * BSV Browser native secp seam — added by patch-package (native-crypto M2).
+ *
+ * The app entry (index.js) installs a Nitro hybrid object (react-native-secp-native,
+ * backed by the rust-secp256k1 staticlib proven byte-exact against this exact SDK
+ * version in native-secp-poc/) at `globalThis.__bsvSecpNative`. When it is absent
+ * (web, jest, node, Expo Go, or any build without the pod) every caller silently
+ * uses the original pure-JS implementation — same probe philosophy as Hash.ts's
+ * node:crypto detection.
+ *
+ * Bytes-only seam: all BigNumber/Point/DER conversion happens HERE, at the
+ * boundary, and nowhere else. No hex strings on the hot path.
+ */
+import * as nobleLib from '@noble/secp256k1';
+import { sha256hmac } from './Hash.js';
+/**
+ * Returns the native module, or null when unavailable. Never throws.
+ */
+export function nativeSecp() {
+    const g = globalThis;
+    const n = g == null ? null : g.__bsvSecpNative;
+    return n == null ? null : n;
+}
+// ── Tier-3 ECDSA backend: @noble/secp256k1 (CR-7, issue #35) ─────────────────
+// An always-on, audited pure-JS secp256k1 that stands in for the Nitro native
+// module on every platform the native path can't reach (web, jest, Expo Go,
+// unlinked builds). Same probe philosophy as `nativeSecp`: never throws — on any
+// failure the callers fall through to the SDK's own pure-JS EC, so noble is an
+// ACCELERATOR, never a dependency. Byte parity vs the SDK (RFC 6979 low-S DER)
+// is gated by jest + the differential corpus.
+let nobleTried = false;
+let nobleBackend = null;
+/** Minimal canonical DER integer: trim leading zeros, prepend 0x00 if MSB set. */
+function derInt(b32) {
+    let i = 0;
+    while (i < b32.length - 1 && b32[i] === 0)
+        i++;
+    let v = b32.subarray(i);
+    if ((v[0] & 0x80) !== 0) {
+        const t = new Uint8Array(v.length + 1);
+        t.set(v, 1);
+        v = t;
+    }
+    return v;
+}
+/** Canonical DER SEQUENCE(INTEGER r, INTEGER s) from two 32-byte big-endian scalars. */
+function derFromRS(r32, s32) {
+    const R = derInt(r32);
+    const S = derInt(s32);
+    const body = 2 + R.length + 2 + S.length; // always < 128 → single-byte lengths
+    const out = new Uint8Array(2 + body);
+    let o = 0;
+    out[o++] = 0x30;
+    out[o++] = body;
+    out[o++] = 0x02;
+    out[o++] = R.length;
+    out.set(R, o);
+    o += R.length;
+    out[o++] = 0x02;
+    out[o++] = S.length;
+    out.set(S, o);
+    return out;
+}
+/**
+ * Returns the noble ECDSA backend, or null if it cannot be initialised. Lazy +
+ * memoised + fully guarded. RFC 6979 HMAC-SHA256 is wired to the SDK's OWN
+ * `sha256hmac` so the deterministic nonce is identical to the SDK DRBG and works
+ * in every runtime (no WebCrypto dependency).
+ */
+export function nobleSecp() {
+    if (nobleTried)
+        return nobleBackend;
+    nobleTried = true;
+    try {
+        const secp = nobleLib;
+        if (secp == null || typeof secp.sign !== 'function' || typeof secp.getPublicKey !== 'function') {
+            nobleBackend = null;
+            return nobleBackend;
+        }
+        if (secp.etc != null && typeof secp.etc.hmacSha256Sync !== 'function') {
+            secp.etc.hmacSha256Sync = (key, ...msgs) => Uint8Array.from(sha256hmac(Array.from(key), Array.from(secp.etc.concatBytes(...msgs))));
+        }
+        nobleBackend = {
+            __noble: true,
+            ecdsaSign(msgBuf, keyBuf) {
+                const sig = secp.sign(new Uint8Array(msgBuf), new Uint8Array(keyBuf), { lowS: true });
+                const c = sig.toCompactRawBytes();
+                return derFromRS(c.subarray(0, 32), c.subarray(32, 64));
+            },
+            pubkeyCreateUncompressed(keyBuf) {
+                return secp.getPublicKey(new Uint8Array(keyBuf), false);
+            },
+            pubkeyCreate(keyBuf) {
+                return secp.getPublicKey(new Uint8Array(keyBuf), true);
+            }
+        };
+    }
+    catch (e) {
+        nobleBackend = null;
+    }
+    return nobleBackend;
+}
+/**
+ * The ECDSA backend the sign / toPublicKey seams route through: the Nitro native
+ * module when present (fastest), else the always-on noble backend, else null →
+ * the caller's pure-JS EC. Never throws.
+ */
+export function secpBackend() {
+    return nativeSecp() ?? nobleSecp();
+}
+/**
+ * Returns the Tier-3 native engine (react-native-engine-native, installed by
+ * index.js at `globalThis.__bsvEngineNative` — native-crypto M5.5), or null
+ * when unavailable or lacking the batch sign entry point. Never throws.
+ */
+export function nativeEngine() {
+    const g = globalThis;
+    const n = g == null ? null : g.__bsvEngineNative;
+    if (n == null || typeof n.batchSignP2pkhInputs !== 'function')
+        return null;
+    return n;
+}
+/**
+ * BigNumber (value < 2^256) → 32-byte big-endian ArrayBuffer.
+ */
+export function bnToBuf32(bn) {
+    return Uint8Array.from(bn.toArray('be', 32)).buffer;
+}
+/**
+ * number[] → ArrayBuffer.
+ */
+export function numsToBuf(nums) {
+    return Uint8Array.from(nums).buffer;
+}
+/**
+ * ArrayBuffer → number[].
+ */
+export function bufToNums(buf) {
+    return Array.from(new Uint8Array(buf));
+}
+let warnedCustomK = false;
+let warnedForceLowS = false;
+/**
+ * ECDSA.sign was called with a customK — the native RFC 6979 path cannot honor
+ * it, so the caller uses the (tested) pure-JS fallback. Warn once, dev builds only.
+ */
+export function warnCustomKOnce() {
+    if (!warnedCustomK && typeof __DEV__ !== 'undefined' && __DEV__) {
+        warnedCustomK = true;
+        console.warn('[NativeSecp] ECDSA.sign called with customK — using pure-JS fallback for such calls (native RFC 6979 path bypassed).');
+    }
+}
+/**
+ * ECDSA.sign was called with forceLowS=false — libsecp256k1 always emits low-S,
+ * so byte-parity with the JS high-S output cannot be guaranteed; the caller uses
+ * the pure-JS fallback. Warn once, dev builds only.
+ */
+export function warnForceLowSOnce() {
+    if (!warnedForceLowS && typeof __DEV__ !== 'undefined' && __DEV__) {
+        warnedForceLowS = true;
+        console.warn('[NativeSecp] ECDSA.sign called with forceLowS=false — using pure-JS fallback for such calls (native path is always low-S).');
+    }
+}
+const warnedEngineReasons = {};
+/**
+ * Transaction.sign could not (or did not) route through the Tier-3 engine and
+ * used the SecpNative-batched / pure-JS fallback tiers instead (gate 6: every
+ * fallback visibly logged in dev builds). Logged once per distinct reason.
+ */
+export function warnEngineFallback(reason) {
+    if (warnedEngineReasons[reason] !== true && typeof __DEV__ !== 'undefined' && __DEV__) {
+        warnedEngineReasons[reason] = true;
+        console.warn(`[EngineNative] Transaction.sign fell back from the engine-routed path: ${reason}`);
+    }
+}
diff --git a/node_modules/@bsv/sdk/dist/esm/src/primitives/PrivateKey.js b/node_modules/@bsv/sdk/dist/esm/src/primitives/PrivateKey.js
index 9f2ff03..94d3bd3 100644
--- a/node_modules/@bsv/sdk/dist/esm/src/primitives/PrivateKey.js
+++ b/node_modules/@bsv/sdk/dist/esm/src/primitives/PrivateKey.js
@@ -1,3 +1,5 @@
+import Point from './Point.js';
+import { nativeSecp, secpBackend, bnToBuf32, numsToBuf, bufToNums } from './NativeSecp.js';
 import BigNumber from './BigNumber.js';
 import PublicKey from './PublicKey.js';
 import Curve from './Curve.js';
@@ -277,6 +279,24 @@ export default class PrivateKey extends BigNumber {
      * const publicKey = privateKey.toPublicKey();
      */
     toPublicKey() {
+        // Native/noble route (BSV Browser patch): libsecp256k1 pubkey_create when
+        // the Nitro module is present, else the always-on @noble/secp256k1 backend
+        // (CR-7, issue #35). M3: prefer the uncompressed variant (0x04‖x‖y,
+        // byte-equal to encode(false)) so the seam never pays the Point.fromX
+        // BigInt-sqrt decompression; fall back to the compressed fn + fromDER on
+        // older native modules. Falls through to pure-JS on any backend failure
+        // (e.g. key === 0 → point at infinity, which noble rejects).
+        const n = secpBackend();
+        if (n != null) {
+            try {
+                if (typeof n.pubkeyCreateUncompressed === 'function') {
+                    const u = bufToNums(n.pubkeyCreateUncompressed(bnToBuf32(this)));
+                    return new PublicKey(u.slice(1, 33), u.slice(33, 65));
+                }
+                return PublicKey.fromDER(bufToNums(n.pubkeyCreate(bnToBuf32(this))));
+            }
+            catch (e) { /* fall through to pure-JS */ }
+        }
         const c = new Curve();
         const p = c.g.mulCT(this);
         return new PublicKey(p.x, p.y);
@@ -365,6 +385,21 @@ export default class PrivateKey extends BigNumber {
         if (!key.validate()) {
             throw new Error('Public key not valid for ECDH secret derivation');
         }
+        // Native route (BSV Browser patch): un-hashed ECDH shared point via
+        // libsecp256k1. M3: prefer the uncompressed variant (no BigInt-sqrt
+        // decompression at the seam). Falls through to pure-JS on any native
+        // failure so edge-case behavior is preserved.
+        const n = nativeSecp();
+        if (n != null) {
+            try {
+                if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                    const u = bufToNums(n.ecdhSharedPointUncompressed(bnToBuf32(this), numsToBuf(key.encode(true))));
+                    return new Point(u.slice(1, 33), u.slice(33, 65));
+                }
+                return Point.fromDER(bufToNums(n.ecdhSharedPoint(bnToBuf32(this), numsToBuf(key.encode(true)))));
+            }
+            catch (e) { /* fall through to pure-JS */ }
+        }
         return key.mulCT(this);
     }
     /**
@@ -403,6 +438,59 @@ export default class PrivateKey extends BigNumber {
      * @returns The derived child key.
      */
     deriveChild(publicKey, invoiceNumber, cacheSharedSecret, retrieveCachedSharedSecret) {
+        // Native route (BSV Browser patch): single-call BRC-42 composite
+        // (ECDH point → HMAC-SHA256 → (k + tweak) mod n) in libsecp256k1 when
+        // no shared-secret cache callbacks are supplied. M3: when cache
+        // callbacks ARE supplied (e.g. CachedKeyDeriver), the caching contract
+        // is honored — the JS Point flows through the callbacks exactly as in
+        // the pure-JS path — while ECDH and the tweak-add stay native (M2 fell
+        // back to pure JS here). Falls through to pure-JS on any native failure
+        // (incl. hmac >= n / child == 0, where the JS mod-n semantics differ).
+        {
+            const n = nativeSecp();
+            if (n != null && typeof invoiceNumber === 'string' && publicKey.validate()) {
+                try {
+                    if (typeof cacheSharedSecret !== 'function' && typeof retrieveCachedSharedSecret !== 'function') {
+                        const child = n.brc42DeriveChild(bnToBuf32(this), numsToBuf(publicKey.encode(true)), invoiceNumber);
+                        return new PrivateKey(bufToNums(child));
+                    }
+                    let sharedSecretN;
+                    if (typeof retrieveCachedSharedSecret === 'function') {
+                        const retrieved = retrieveCachedSharedSecret(this, publicKey);
+                        if (retrieved === undefined) {
+                            if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                                const u = bufToNums(n.ecdhSharedPointUncompressed(bnToBuf32(this), numsToBuf(publicKey.encode(true))));
+                                sharedSecretN = new Point(u.slice(1, 33), u.slice(33, 65));
+                            }
+                            else {
+                                sharedSecretN = Point.fromDER(bufToNums(n.ecdhSharedPoint(bnToBuf32(this), numsToBuf(publicKey.encode(true)))));
+                            }
+                            if (typeof cacheSharedSecret === 'function') {
+                                cacheSharedSecret(this, publicKey, sharedSecretN);
+                            }
+                        }
+                        else {
+                            sharedSecretN = retrieved;
+                        }
+                    }
+                    else {
+                        // only cacheSharedSecret supplied: compute natively, cache
+                        if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                            const u = bufToNums(n.ecdhSharedPointUncompressed(bnToBuf32(this), numsToBuf(publicKey.encode(true))));
+                            sharedSecretN = new Point(u.slice(1, 33), u.slice(33, 65));
+                        }
+                        else {
+                            sharedSecretN = Point.fromDER(bufToNums(n.ecdhSharedPoint(bnToBuf32(this), numsToBuf(publicKey.encode(true)))));
+                        }
+                        cacheSharedSecret(this, publicKey, sharedSecretN);
+                    }
+                    const hmacN = sha256hmac(sharedSecretN.encode(true), toArray(invoiceNumber, 'utf8'));
+                    const child = n.privkeyTweakAdd(bnToBuf32(this), numsToBuf(hmacN));
+                    return new PrivateKey(bufToNums(child));
+                }
+                catch (e) { /* fall through to pure-JS */ }
+            }
+        }
         let sharedSecret;
         if (typeof retrieveCachedSharedSecret === 'function') {
             const retrieved = retrieveCachedSharedSecret(this, publicKey);
diff --git a/node_modules/@bsv/sdk/dist/esm/src/primitives/PublicKey.js b/node_modules/@bsv/sdk/dist/esm/src/primitives/PublicKey.js
index 9b9f52c..bb187fa 100644
--- a/node_modules/@bsv/sdk/dist/esm/src/primitives/PublicKey.js
+++ b/node_modules/@bsv/sdk/dist/esm/src/primitives/PublicKey.js
@@ -5,6 +5,7 @@ import BigNumber from './BigNumber.js';
 import { sha256, sha256hmac, hash160 } from './Hash.js';
 import Signature from './Signature.js';
 import { toArray, toBase58Check, toHex } from './utils.js';
+import { nativeSecp, bnToBuf32, numsToBuf, bufToNums } from './NativeSecp.js';
 /**
  * The PublicKey class extends the Point class. It is used in public-key cryptography to derive shared secret, verify message signatures, and encode the public key in the DER format.
  * The class comes with static methods to generate PublicKey instances from private keys or from strings.
@@ -103,6 +104,21 @@ export default class PublicKey extends Point {
         if (!this.validate()) {
             throw new Error('Public key not valid for ECDH secret derivation');
         }
+        // Native route (BSV Browser patch): un-hashed ECDH shared point via
+        // libsecp256k1. M3: prefer the uncompressed variant (no BigInt-sqrt
+        // decompression at the seam). Falls through to pure-JS on any native
+        // failure.
+        const n = nativeSecp();
+        if (n != null) {
+            try {
+                if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                    const u = bufToNums(n.ecdhSharedPointUncompressed(bnToBuf32(priv), numsToBuf(this.encode(true))));
+                    return new Point(u.slice(1, 33), u.slice(33, 65));
+                }
+                return Point.fromDER(bufToNums(n.ecdhSharedPoint(bnToBuf32(priv), numsToBuf(this.encode(true)))));
+            }
+            catch (e) { /* fall through to pure-JS */ }
+        }
         return this.mulCT(priv);
     }
     /**
@@ -190,6 +206,68 @@ export default class PublicKey extends Point {
      * @returns The derived child key.
      */
     deriveChild(privateKey, invoiceNumber, cacheSharedSecret, retrieveCachedSharedSecret) {
+        // Native route (BSV Browser patch): BRC-42 public-side derivation.
+        // M3: when no cache callbacks are supplied, ONE native composite call
+        // (brc42DeriveChildPubUncompressed: ECDH → HMAC → P + t·G, uncompressed
+        // out — no decompression, one crossing instead of three). When cache
+        // callbacks ARE supplied (e.g. CachedKeyDeriver), the caching contract
+        // is honored with the JS Point flowing through the callbacks while the
+        // point math stays native. Falls through to pure-JS on any native failure.
+        {
+            const n = nativeSecp();
+            if (n != null && typeof invoiceNumber === 'string' && this.validate()) {
+                try {
+                    const self33 = numsToBuf(this.encode(true));
+                    if (typeof cacheSharedSecret !== 'function' && typeof retrieveCachedSharedSecret !== 'function') {
+                        if (typeof n.brc42DeriveChildPubUncompressed === 'function') {
+                            const u = bufToNums(n.brc42DeriveChildPubUncompressed(bnToBuf32(privateKey), self33, invoiceNumber));
+                            return new PublicKey(u.slice(1, 33), u.slice(33, 65));
+                        }
+                        const shared = bufToNums(n.ecdhSharedPoint(bnToBuf32(privateKey), self33));
+                        const hmac = sha256hmac(shared, toArray(invoiceNumber, 'utf8'));
+                        const tweaked = n.pubkeyTweakAdd(self33, numsToBuf(hmac));
+                        return PublicKey.fromDER(bufToNums(tweaked));
+                    }
+                    let sharedSecretN;
+                    if (typeof retrieveCachedSharedSecret === 'function') {
+                        const retrieved = retrieveCachedSharedSecret(privateKey, this);
+                        if (retrieved === undefined) {
+                            if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                                const u = bufToNums(n.ecdhSharedPointUncompressed(bnToBuf32(privateKey), self33));
+                                sharedSecretN = new Point(u.slice(1, 33), u.slice(33, 65));
+                            }
+                            else {
+                                sharedSecretN = Point.fromDER(bufToNums(n.ecdhSharedPoint(bnToBuf32(privateKey), self33)));
+                            }
+                            if (typeof cacheSharedSecret === 'function') {
+                                cacheSharedSecret(privateKey, this, sharedSecretN);
+                            }
+                        }
+                        else {
+                            sharedSecretN = retrieved;
+                        }
+                    }
+                    else {
+                        if (typeof n.ecdhSharedPointUncompressed === 'function') {
+                            const u = bufToNums(n.ecdhSharedPointUncompressed(bnToBuf32(privateKey), self33));
+                            sharedSecretN = new Point(u.slice(1, 33), u.slice(33, 65));
+                        }
+                        else {
+                            sharedSecretN = Point.fromDER(bufToNums(n.ecdhSharedPoint(bnToBuf32(privateKey), self33)));
+                        }
+                        cacheSharedSecret(privateKey, this, sharedSecretN);
+                    }
+                    const hmacN = sha256hmac(sharedSecretN.encode(true), toArray(invoiceNumber, 'utf8'));
+                    if (typeof n.pubkeyTweakAddUncompressed === 'function') {
+                        const u = bufToNums(n.pubkeyTweakAddUncompressed(self33, numsToBuf(hmacN)));
+                        return new PublicKey(u.slice(1, 33), u.slice(33, 65));
+                    }
+                    const tweakedN = n.pubkeyTweakAdd(self33, numsToBuf(hmacN));
+                    return PublicKey.fromDER(bufToNums(tweakedN));
+                }
+                catch (e) { /* fall through to pure-JS */ }
+            }
+        }
         let sharedSecret;
         if (typeof retrieveCachedSharedSecret === 'function') {
             const retrieved = retrieveCachedSharedSecret(privateKey, this);
diff --git a/node_modules/@bsv/sdk/dist/esm/src/primitives/Schnorr.js b/node_modules/@bsv/sdk/dist/esm/src/primitives/Schnorr.js
index 71f5213..0df629f 100644
--- a/node_modules/@bsv/sdk/dist/esm/src/primitives/Schnorr.js
+++ b/node_modules/@bsv/sdk/dist/esm/src/primitives/Schnorr.js
@@ -2,6 +2,9 @@ import BigNumber from './BigNumber.js';
 import Curve from './Curve.js';
 import { sha256 } from './Hash.js';
 import PrivateKey from './PrivateKey.js';
+import Point from './Point.js';
+import PublicKey from './PublicKey.js';
+import { nativeSecp, bnToBuf32, numsToBuf, bufToNums } from './NativeSecp.js';
 /**
  * Class representing the Schnorr Zero-Knowledge Proof (ZKP) protocol.
  *
@@ -46,6 +49,27 @@ export default class Schnorr {
      * @returns Proof (R, S', z)
      */
     generateProof(aArg, AArg, BArg, S) {
+        // Native route (BSV Browser patch): one flow call into libsecp256k1.
+        // The nonce r is drawn HERE from PrivateKey.fromRandom — the identical
+        // source the SDK uses — and passed across the seam so the native fn
+        // stays deterministic and vector-testable. Guard: a in [1, n-1]
+        // (out-of-range/negative scalars keep exact SDK BigNumber semantics
+        // via the pure-JS path). Any native throw falls through to pure JS.
+        {
+            const nat = nativeSecp();
+            if (nat != null && aArg.cmpn(0) > 0 && aArg.cmp(this.curve.n) < 0) {
+                try {
+                    const rNonce = PrivateKey.fromRandom();
+                    const proof = bufToNums(nat.schnorrGenerateProof(bnToBuf32(aArg), numsToBuf(AArg.encode(true)), numsToBuf(BArg.encode(true)), numsToBuf(S.encode(true)), bnToBuf32(rNonce)));
+                    return {
+                        R: PublicKey.fromDER(proof.slice(0, 33)),
+                        SPrime: Point.fromDER(proof.slice(33, 66)),
+                        z: new BigNumber(proof.slice(66, 98))
+                    };
+                }
+                catch (err) { /* fall through to pure-JS */ }
+            }
+        }
         const r = PrivateKey.fromRandom();
         const R = r.toPublicKey();
         const SPrime = BArg.mul(r);
@@ -63,6 +87,22 @@ export default class Schnorr {
      */
     verifyProof(A, B, S, proof) {
         const { R, SPrime, z } = proof;
+        // Native route (BSV Browser patch): one flow call replaces four JS
+        // point multiplications. Routed ONLY for z in [1, n-1] — MEASURED: the
+        // SDK's Point.mul is not consistently (z mod n)*G for out-of-range
+        // scalars (g.mul(n+1) matches the reduced value, g.mul(2^256-1) does
+        // not), so pre-reducing z would change the SDK's verdict; out-of-range
+        // z uses the pure-JS path. Any native throw (invalid encodings, point
+        // at infinity in encode) also falls through.
+        {
+            const nat = nativeSecp();
+            if (nat != null && z != null && z.cmpn(0) > 0 && z.cmp(this.curve.n) < 0) {
+                try {
+                    return nat.schnorrVerifyProof(numsToBuf(A.encode(true)), numsToBuf(B.encode(true)), numsToBuf(S.encode(true)), numsToBuf(R.encode(true)), numsToBuf(SPrime.encode(true)), bnToBuf32(z));
+                }
+                catch (err) { /* fall through to pure-JS */ }
+            }
+        }
         const e = this.computeChallenge(A, B, S, SPrime, R);
         // Check zG = R + eA
         const zG = this.curve.g.mul(z);
diff --git a/node_modules/@bsv/sdk/dist/esm/src/primitives/Signature.js b/node_modules/@bsv/sdk/dist/esm/src/primitives/Signature.js
index da97bed..5792973 100644
--- a/node_modules/@bsv/sdk/dist/esm/src/primitives/Signature.js
+++ b/node_modules/@bsv/sdk/dist/esm/src/primitives/Signature.js
@@ -5,6 +5,7 @@ import { sha256 } from './Hash.js';
 import { toArray, toHex, toBase64 } from './utils.js';
 import Point from './Point.js';
 import Curve from './Curve.js';
+import { nativeSecp, bnToBuf32, numsToBuf, bufToNums } from './NativeSecp.js';
 /**
  * Represents a digital signature.
  *
@@ -295,6 +296,34 @@ export default class Signature {
      * const publicKey = signature.RecoverPublicKey(0, msgHash);
      */
     RecoverPublicKey(recovery, e) {
+        // Native route (BSV Browser patch): libsecp256k1 recovery over the SDK
+        // compact layout ([31+recid, r32, s32]). Guards keep unsigned 32-byte
+        // semantics (negative r/s/e keep the SDK's BigNumber behavior via the
+        // JS path). Any native throw — including the recid-2/3 r + n >= p field
+        // overflow the SDK resolves by Point.fromX mod-p reduction — falls
+        // through to the pure-JS path, preserving exact SDK behavior.
+        {
+            const nat = nativeSecp();
+            if (nat != null &&
+                Number.isInteger(recovery) && recovery >= 0 && recovery <= 3 &&
+                this.r.cmpn(0) >= 0 && this.s.cmpn(0) >= 0 && e.cmpn(0) >= 0) {
+                try {
+                    const compact = new Uint8Array(65);
+                    compact[0] = 31 + recovery;
+                    compact.set(this.r.toArray('be', 32), 1);
+                    compact.set(this.s.toArray('be', 32), 33);
+                    // M3: prefer the uncompressed variant — no BigInt-sqrt
+                    // decompression at the seam.
+                    if (typeof nat.ecdsaRecoverUncompressed === 'function') {
+                        const u = bufToNums(nat.ecdsaRecoverUncompressed(bnToBuf32(e), compact.buffer));
+                        return new PublicKey(u.slice(1, 33), u.slice(33, 65));
+                    }
+                    const pk = nat.ecdsaRecover(bnToBuf32(e), compact.buffer);
+                    return PublicKey.fromDER(bufToNums(pk));
+                }
+                catch (err) { /* fall through to pure-JS */ }
+            }
+        }
         const r = this.r;
         const s = this.s;
         // A set LSB signifies that the y-coordinate is odd
@@ -340,6 +369,22 @@ export default class Signature {
      * const recovery = signature.CalculateRecoveryFactor(publicKey, msgHash);
      */
     CalculateRecoveryFactor(pubkey, msgHash) {
+        // Native route (BSV Browser patch): one native call replaces up to four
+        // JS point-recovery rounds (same 0..3 search order, same equality). A
+        // native miss (incl. the recid-2/3 asymmetry, where the SDK may match a
+        // mod-p fabricated point) falls through to the pure-JS loop.
+        {
+            const nat = nativeSecp();
+            if (nat != null && this.r.cmpn(0) >= 0 && this.s.cmpn(0) >= 0 && msgHash.cmpn(0) >= 0) {
+                try {
+                    const sig64 = new Uint8Array(64);
+                    sig64.set(this.r.toArray('be', 32), 0);
+                    sig64.set(this.s.toArray('be', 32), 32);
+                    return nat.ecdsaRecoveryFactor(bnToBuf32(msgHash), sig64.buffer, numsToBuf(pubkey.encode(true)));
+                }
+                catch (err) { /* fall through to pure-JS */ }
+            }
+        }
         for (let recovery = 0; recovery < 4; recovery++) {
             let Qprime;
             try {
