# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/.windsurfrules

.env

# Expo development build files
*.apk
*.ipa
android
# Workspace packages ship their own tracked Android native module sources
# (Nitro modules autolinked on Android) under packages/*/android — keep those
# out of the bare `android` rule above, which exists to ignore the top-level
# Expo-generated /android app dir and any incidental `android` directories
# elsewhere (e.g. credentials/android, which must stay ignored).
!/packages/*/android
!/packages/*/android/**

# ...but still ignore Gradle/CMake build output nested inside those tracked
# package android/ dirs.
packages/*/android/build
packages/*/android/.cxx

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
.expo
*.aab
/BSVBrowser.app
build-*.tar.gz
/@bsvb__bsv-browser.jks
docs/marketing
marketing.html
Dev Builds.zip
.opencode
credentials.json
.superpowers/
